version: '3.8'
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: flask_container
    ports:
      - "5000:5000"
    mem_limit: 256m
    cpu_shares: 128


